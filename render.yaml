services:
  - type: web
    name: adopcion-app
    env: dotnet
    plan: free
    branch: deploy/rende
    buildCommand: dotnet publish -c Release -o out
    startCommand: dotnet out/practica2.dll
    workingDir: practica2
